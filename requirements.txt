python-dotenv>=1.0.0
pydantic>=2.0.0

# Phase 1+ (declared early for consistent environment)
pandas>=2.0.0
datasets>=2.14.0

# Phase 6/7 — backend API for Next.js frontend (deployed on Railway)
fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.27.0

# Development
pytest>=7.4.0
